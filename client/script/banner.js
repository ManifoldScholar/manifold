#!/usr/bin/env node

const cfonts = require("cfonts");

cfonts.say("Manifold", {
  font: "block",
  align: "left",
  colors: ["greenBright", "green"],
  background: "transparent",
  letterSpacing: 1,
  lineHeight: 1,
  maxLength: 0,
  space: true
});
