import React, { Component, PropTypes } from 'react';

export default class ProjectResources extends Component {
  render() {
    return (
      <div>
        {'All resources here!'}
      </div>
    );
  }
}
