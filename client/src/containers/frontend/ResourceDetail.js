import React, { Component, PropTypes } from 'react';

export default class ResourceDetail extends Component {
  render() {
    return (
        <div>
          {'Resource Detail'}
        </div>
    );
  }
}
