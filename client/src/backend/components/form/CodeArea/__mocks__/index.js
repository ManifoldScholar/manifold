import React, { Component } from "react";
import setter from "../../setter";

class FormCodeArea extends Component {
  render() {
    return <div>This is the CodeArea mock</div>;
  }
}

export default setter(FormCodeArea);
