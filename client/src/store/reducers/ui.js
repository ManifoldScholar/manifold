import {combineReducers} from 'redux';
import filters from './ui/filters';
import typography from './ui/typography';
import visibility from './ui/visibility';

export default combineReducers({filters, typography, visibility});
